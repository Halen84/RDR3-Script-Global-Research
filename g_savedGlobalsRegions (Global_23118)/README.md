<h2>g_savedGlobalsRegions</h2>

```
struct g_savedGlobalsRegions
{
	int iInnocentsKilled = *getGlobalPtr(BASE + 1768);

private:
	static const int BASE = 23118;
};
```

# Snippets from the scripts (b1436)

```